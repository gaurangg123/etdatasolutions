version: '3.8'

services:
  web:
    build: .
    container_name: etds_web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    networks:
      - etds_net

networks:
  etds_net:
    driver: bridge
